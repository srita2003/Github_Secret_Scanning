name: Secret Scan Pipeline

on: [push]

jobs:
  secret-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run Secret Scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./